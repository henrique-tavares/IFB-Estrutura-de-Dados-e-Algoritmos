#pragma once
#include "../ListaBaseInt.h"

/** 
 * Questão 8
 * Função maior_elemento
 * Retorna por referência o valor e a posição do maior elemento da lista
 * Retorna se foi possível a operação
 */
bool maior_elemento(Lista *lista, int *maior, int *pos);
