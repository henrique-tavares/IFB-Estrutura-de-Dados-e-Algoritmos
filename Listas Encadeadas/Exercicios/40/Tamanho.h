#pragma once
#include "../ListaBaseInt.h"

// Questão 40

/**
 * Função tamanho
 * Retorna o número de elementos de uma lista
 */
int tamanho(Lista *lista);

/**
 * Função tamanho_recurssivo
 * Retorna o número de elementos de uma lista
 * recurssivamente
 */
int tamanho_recurssivo(Lista *lista);
